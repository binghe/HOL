all: $(DEFAULT_TARGETS)
.PHONY: all selftest.exe

selftest.exe: selftest.uo reduceLib.uo
	$(HOLMOSMLC) -o $@ $<

EXTRA_CLEANS = selftest.exe reduce-selftest.log

ifdef HOLSELFTESTLEVEL
all: reduce-selftest.log

reduce-selftest.log: selftest.exe
	$(tee ./$<,$@)
endif

